html 
 title Results of Similar Categories
 include ../partials/header.pug

 main 
  body 
   h4 Similar Search Results of Artworks with the category of #{search}:
   each art in artworks 
     a(href="http://localhost:3000/" + art.name)
       li #{art.name}